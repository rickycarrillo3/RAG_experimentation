# ── User data — never commit ───────────────────────────────────────────────────
# ChromaDB stores embedded document content (user private data)
**/chroma_db/
# BM25 indexes contain user document text
**/bm25_indexes/
# PDFs anywhere in the repo (copyrighted textbooks or personal documents)
**/*.pdf
# Raw PDF staging dir
**/docs/raw/
# Nougat-extracted markdown (derived from private PDFs)
**/docs/extracted/
# Any pickle files (may contain serialized user data)
**/*.pkl

# ── Secrets & config ───────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem
secrets.yaml
secrets.json

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# ── Model cache (large, not source code) ───────────────────────────────────────
**/.cache/
**/model_cache/
**/*.bin
**/*.safetensors
**/*.gguf

# ── OS & editor ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Docs placeholder — keep the empty dirs in git ─────────────────────────────
!**/docs/.gitkeep
!**/docs/raw/.gitkeep
!**/docs/extracted/.gitkeep

# ── Plan file (local only) ─────────────────────────────────────────────────────
build_plan.md
math_chat_keys/
# Eval gold sets + results embed user document text (private, like docs/ and chroma_db/)
knowledge-base-math/eval/
