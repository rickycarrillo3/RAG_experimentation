# Pinned <2.0 deliberately. marker-pdf 2.0.0 (2026-07-20) stopped running Surya
# in-process: it now spawns an inference server on first use — vLLM **in Docker** on
# NVIDIA GPUs. A RunPod pod is itself a container with no Docker daemon, so every
# extraction dies with "docker binary not found" and falls back to pymupdf4llm, which
# produces NO LaTeX (CLAUDE.md). It surfaces only on the first upload: prefetch's
# create_model_dict() succeeds because the Docker spawn happens at conversion time,
# not at model load. See ERRORS.md.
marker-pdf<2.0
pymupdf4llm
langchain-core
langchain-community
langchain-text-splitters
langchain-huggingface
langchain-chroma
langchain-ollama
chromadb
rank-bm25
# Pinned together with gradio below — the two are coupled through huggingface-hub.
# sentence-transformers 6.0.0 requires huggingface-hub>=1.3.0, which the marker-pdf<2.0
# world cannot provide (see the gradio comment). 5.5.1 accepts hub>=0.23.0 and is the
# version the reranker scale in api/settings.py was validated against.
sentence-transformers==5.5.1
pylatexenc
# 6.17.3, NOT 6.18.0. This is a three-way pin, not a preference:
#   marker-pdf<2.0 (above) -> transformers<5.0.0 -> huggingface-hub<1.0
#   gradio 6.18.0          -> huggingface-hub>=1.2.0
# Those are unsatisfiable together, and pip does not say so. It backtracks through
# ~30 sentence-transformers versions hunting for an escape that does not exist, stalls
# for minutes, then fails with ResolutionImpossible pointing at sentence-transformers —
# a package that is not the problem. 6.17.3 is the last release accepting hub>=0.33.5.
# If marker-pdf<2.0 is ever lifted, this can move forward again. See ERRORS.md.
gradio==6.17.3
torch
# torchvision is a marker-pdf dependency, listed explicitly because it is a trap:
# it ships compiled C++ ops linked against one exact torch build. Installed from PyPI
# (which is what happens transitively) it will not match a torch installed from a CUDA
# index, and every torchvision import dies with "operator torchvision::nms does not
# exist". They must come from the SAME index, as a pair — see SETUP.md §1, where the
# CUDA install deliberately runs *after* this file for that reason.
torchvision
# API service
fastapi
uvicorn[standard]
python-multipart   # required by FastAPI for the multipart /upload form
httpx              # SSE client for app.py + Ollama health probe + RunPod stop call
